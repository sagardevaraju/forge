# Offline CV training deps (not bundled into Vercel functions).
# Install on the training workstation only:
#   conda activate forge
#   pip install -r requirements.txt -r requirements-train.txt

torch>=2.2
torchvision>=0.17
timm>=1.0
transformers>=4.40
