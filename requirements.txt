numpy>=1.26
pytest>=8.0
requests>=2.31
scipy>=1.11  # Task P2.1: continuous CRPS (PchipInterpolator + quad)
shapely>=2.0  # Task SIM.6: polygon containment + perturbation for sim loss generator

# Task 10-11: loss synthesis + XGBoost quantile training
pandas>=2.0
pyarrow>=14.0
xgboost>=2.0
optuna>=3.5
scikit-learn>=1.4
joblib>=1.3

# Task 16-17: optimization (Portfolio MIP + Operational VRP)
pulp>=2.7
ortools>=9.10

# Task 24-25: component- and decision-level evaluation
matplotlib>=3.8

# Optional — only required for real-mode Sentinel-2 fetches (FORGE_CV_MODE=real)
planetary-computer>=1.0
pystac-client>=0.7
rasterio>=1.3
rioxarray>=0.15
